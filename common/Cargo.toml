[package]
name = "super-tux-showdown-common"
version = "0.1.0"
edition.workspace = true

[dependencies]
bevy_math = "0.18.1"
serde = { version = "1", features = ["derive"] }
serde_with = { version = "3", features = ["base64"] }
